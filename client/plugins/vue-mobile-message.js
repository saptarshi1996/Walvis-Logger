import Vue from 'vue'
import { MessageBox, Toast, Loading } from 'vue-mobile-message'

Vue.use(MessageBox)    //( this.$alert | this.$confirm )
Vue.use(Toast)         //( this.$toast )
Vue.use(Loading)
